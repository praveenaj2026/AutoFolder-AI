# AutoFolder AI - Default Configuration

app:
  name: "AutoFolder AI"
  version: "1.0.0"
  mode: "base"  # base or pro
  language: "en"

safety:
  preview_required: true
  undo_enabled: true
  max_undo_history: 10
  dry_run_default: true
  never_delete: true
  backup_before_organize: false

organization:
  default_folder: "Downloads"
  create_folders_if_missing: true
  handle_conflicts: "rename"  # rename, skip, or overwrite
  preserve_timestamps: true
  ignore_hidden_files: true

duplicates:
  enabled: true
  auto_detect: false  # Automatically scan for duplicates before organizing
  default_action: "keep_newest"  # keep_newest, keep_oldest, keep_all, skip
  move_duplicates_to: "Duplicates"  # Folder name for 'keep_all' action
  hash_algorithm: "sha256"  # sha256 or md5

smart_rename:
  enabled: false  # Feature disabled - manual rename script available
  auto_suggest: false
    - "Untitled"
  
ai:
  enabled: true  # AI always enabled in v2.0
  model_path: "C:/Users/Praveen/Downloads/Python Scripts/AI Model"  # Cache folder with existing model
  use_local_only: true
  embedding_model: "sentence-transformers/all-MiniLM-L6-v2"  # Full model path for offline use
  confidence_threshold: 0.7
  max_suggestions: 5

# Phase 3.7: Content Analysis for improved AI accuracy
content_analysis:
  enabled: true
  analyze_pdfs: true  # Extract text from PDF files
  ocr_images: true    # Use OCR on scanned documents/images
  detect_document_types: true  # Detect Resume, Invoice, Contract, etc.
  ocr_language: "eng"  # Tesseract language code
  max_file_size_mb: 50  # Skip files larger than this
  cache_results: true   # Cache extracted content

# Phase 3.7: AI Learning System - Track corrections to improve accuracy
ai_learning:
  enabled: true
  collect_corrections: true  # Track user corrections to AI groups
  auto_export: false         # Auto-export training data
  data_folder: null          # Null = use default logs folder

# Phase 3.7: Smart Compression - Compress old/large files
compression:
  enabled: true
  default_method: "zip"      # zip, zip_max, 7z, 7z_max
  days_old_threshold: 90     # Suggest files older than this
  min_size_mb: 1             # Only suggest files larger than this
  skip_extensions:           # Never compress these
    - ".zip"
    - ".7z"
    - ".rar"
    - ".gz"
    - ".mp4"
    - ".mkv"
    - ".mp3"
  delete_originals: false    # Delete files after compression

ui:
  theme: "light"  # light or dark
  model_path: "models"  # Relative cache folder (bundled in EXE build)
  confirm_before_organize: true
  show_file_count: true
  window_width: 1200
  window_height: 800

logging:
  enabled: true
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  log_file: "logs/autofolder.log"
  max_log_size_mb: 10
  backup_count: 3

performance:
  max_files_per_batch: 1000
  use_threading: true
  preview_timeout_seconds: 30
